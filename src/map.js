import _map from "./internal/_map";

/**
 * @description
 * <span style='color:#ff4d4f;font-size:18px;font-weight:bold;'>通过转换函数 (iteratee) 映射列表中的每个值来生成新的值数组。iteratee 会传递三个参数：值、迭代的索引（或键），最后是整个列表的引用。</span>
 * |输入值|输出值
 * |---|---|
 * |*|*
 * @function map
 * @param {Array|Object} list - 数组或者对象
 * @return {Array|Object} - 返回转换后的新的数组或者对象
 * @version 1.0.0
 */
const map = _map;

export default map;
