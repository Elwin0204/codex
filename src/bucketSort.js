import _bucketSort from './internal/_bucketSort';

/**
 * @description
 * <span style='color:#ff4d4f;font-size:18px;font-weight:bold;'>桶排序。</span>
 * |输入值|输出值
 * |---|---|
 * |*|*
 * @function bucketSort
 * @param {*} arr - 数组
 * @return {*} - 返回排好序的数组
 * @version 1.0.0
 */
const bucketSort = _bucketSort;

export default bucketSort;